import { NoPastePasswordDirective } from './no-paste-password.directive';

describe('NoPastePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new NoPastePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
